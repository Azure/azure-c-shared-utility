name: $(BuildID)_$(BuildDefinitionName)_$(SourceBranchName)_$(Date:yyyyMMdd)$(Rev:.r)

# NOTE: This pipeline was migrated off the internal csdkcontainerregistry
# container images and the self-hosted 'sdk-c--*' agent pools (which are no
# longer available) onto Microsoft-hosted vmImage agents, mirroring the
# structural fixes applied to azure-iot-sdk-c/build/.vsts-ci.yml.
# c-utility has no E2E tests (only unit tests), so the IoT Hub / DPS secret
# env blocks that used to be plumbed into every job have been removed.

resources:
  - repo: self
    clean: true

jobs:
- job: checksubmodule
  variables:
    CodeQL.Enabled: false
  pool:
    vmImage: 'ubuntu-24.04'
  displayName: 'Check Submodules'
  steps:
  - checkout: self
    submodules: recursive
  - script: |
      sudo apt-get update && sudo apt-get install -y \
      curl \
      git \
      build-essential \
      pkg-config
      sudo curl -sL https://deb.nodesource.com/setup_16.x | bash -
      sudo apt-get install -y nodejs
    displayName: 'Setup'
  - script: |
      npm install check_submodules
      node_modules/.bin/check_submodules . master
    displayName: 'Check Submodules Match'

- job: windowsx86
  timeoutInMinutes: 90
  pool:
    vmImage: 'windows-2022'
  displayName: 'Windows x86'
  # Disabled pending PR to add x86 stub for c-logging get_thread_stack.
  # See: https://github.com/ewertons/c-logging/tree/ewertons/x86-get-thread-stack-stub
  condition: false
  steps:
  - checkout: self
    submodules: true
  - script: |
      call jenkins\windows_c.cmd
    displayName: 'Build'
  - script: |
      call jenkins\windows_c_VsDevCmd.cmd x86 && cd cmake && ctest -T test --no-compress-output -C Debug -V -j 16 --schedule-random
    displayName: 'Run Tests'
  - task: PublishTestResults@2
    displayName: 'Publish Windows x86 Results'
    inputs:
      testRunner: CTest
      testResultsFiles: '**/Test.xml'
      mergeTestResults: true
      testRunTitle: 'windowsx86'
    condition: succeededOrFailed()
  - task: ComponentGovernanceComponentDetection@0
    displayName: 'Component Detection'

- job: windowsx64
  timeoutInMinutes: 120
  variables:
    CodeQL.Enabled: $[eq(variables['Build.SourceBranch'], 'refs/heads/master')]
    CodeQL.Language: cpp
  pool:
    vmImage: 'windows-2022'
  displayName: 'Windows x64'
  steps:
  - checkout: self
    submodules: true
  - task: CodeQL3000Init@0
  - script: |
      call jenkins\windows_c.cmd --platform x64
    displayName: 'Build'
  - task: CodeQL3000Finalize@0
    condition: always()
  - script: |
      call jenkins\windows_c_VsDevCmd.cmd x64 && cd cmake && ctest -T test --no-compress-output -C Debug -V -j 16 --schedule-random
    displayName: 'Run Tests'
  - task: PublishTestResults@2
    displayName: 'Publish Windows x64 Results'
    inputs:
      testRunner: CTest
      testResultsFiles: '**/Test.xml'
      mergeTestResults: true
      testRunTitle: 'windowsx64'
    condition: succeededOrFailed()
  - task: ComponentGovernanceComponentDetection@0
    displayName: 'Component Detection'

- job: windowsdynamic
  timeoutInMinutes: 90
  pool:
    vmImage: 'windows-2022'
  displayName: 'Windows Dynamic'
  steps:
  - checkout: self
    submodules: true
  - script: |
      call jenkins\windows_c_VsDevCmd.cmd && call jenkins\windows_c_build_as_dynamic.cmd
    displayName: 'Build'
  - task: ComponentGovernanceComponentDetection@0
    displayName: 'Component Detection'

- job: clang
  timeoutInMinutes: 90
  pool:
    vmImage: 'ubuntu-24.04'
  displayName: 'Linux Ubuntu Clang'
  steps:
  - checkout: self
    submodules: true
  - script: |
      set -e
      sudo apt-get update
      sudo apt-get install -y \
        clang \
        cmake \
        build-essential \
        pkg-config \
        curl \
        libcurl4-openssl-dev \
        libssl-dev \
        uuid-dev
    displayName: 'Host setup'
  - script: |
      export OPENSSL_ia32cap=0x00000000
      sudo chmod -R 755 .
      sudo -E ./jenkins/ubuntu_clang.sh
    displayName: 'Build'
  - script: |
      cd cmake && sudo -E ../build_all/linux/run_tests.sh
    displayName: 'Run Tests'
  - task: PublishTestResults@2
    displayName: 'Publish Clang Results'
    inputs:
      testRunner: CTest
      testResultsFiles: '**/Test.xml'
      mergeTestResults: true
      testRunTitle: 'clang'
    condition: succeededOrFailed()

- job: ubuntu2404
  timeoutInMinutes: 90
  pool:
    vmImage: 'ubuntu-24.04'
  displayName: 'Linux Ubuntu 24.04'
  steps:
  - checkout: self
    submodules: true
  - script: |
      set -e
      sudo apt-get update
      sudo apt-get install -y \
        cmake \
        build-essential \
        pkg-config \
        curl \
        libcurl4-openssl-dev \
        libssl-dev \
        uuid-dev \
        valgrind
    displayName: 'Host setup'
  - script: |
      export OPENSSL_ia32cap=0x00000000
      sudo chmod -R 755 .
      sudo -E ./jenkins/ubuntu_c.sh
    displayName: 'Build'
  - script: |
      cd cmake && sudo -E ../build_all/linux/run_tests.sh
    displayName: 'Run Tests'
  - task: PublishTestResults@2
    displayName: 'Publish Ubuntu 24.04 Results'
    inputs:
      testRunner: CTest
      testResultsFiles: '**/Test.xml'
      mergeTestResults: true
      testRunTitle: 'ubuntu2404'
    condition: succeededOrFailed()

- job: linuxoptions
  timeoutInMinutes: 60
  pool:
    vmImage: 'ubuntu-24.04'
  displayName: 'Linux Ubuntu with Options'
  steps:
  - checkout: self
    submodules: true
  - script: |
      set -e
      sudo apt-get update
      sudo apt-get install -y \
        cmake \
        build-essential \
        pkg-config \
        curl \
        libcurl4-openssl-dev \
        libssl-dev \
        uuid-dev
    displayName: 'Host setup'
  - script: 'sudo ./jenkins/linux_c_option_test.sh'
    displayName: 'Build'
    condition: always()

- job: linux_installed_deps
  timeoutInMinutes: 60
  pool:
    vmImage: 'ubuntu-24.04'
  displayName: 'Linux with Installed Deps'
  steps:
  - checkout: self
    submodules: true
  - script: |
      set -e
      sudo apt-get update
      sudo apt-get install -y \
        cmake \
        build-essential \
        pkg-config \
        curl \
        libcurl4-openssl-dev \
        libssl-dev \
        uuid-dev
    displayName: 'Host setup'
  - script: |
      export OPENSSL_ia32cap=0x00000000
      sudo chmod -R 755 .
      sudo -E ./jenkins/linux_install_deps.sh
    displayName: 'Build'

- job: cares
  timeoutInMinutes: 60
  pool:
    vmImage: 'ubuntu-24.04'
  displayName: 'Linux with C-Ares'
  steps:
  - checkout: self
    submodules: true
  - script: |
      set -e
      sudo apt-get update
      sudo apt-get install -y \
        cmake \
        build-essential \
        pkg-config \
        curl \
        libcurl4-openssl-dev \
        libssl-dev \
        uuid-dev \
        libc-ares-dev
    displayName: 'Host setup'
  - script: |
      sudo -E bash jenkins/linux_cares.sh
    displayName: 'Build'
  - script: |
      cd cmake && sudo -E ../build_all/linux/run_tests.sh
    displayName: 'Run Tests'
  - task: PublishTestResults@2
    displayName: 'Publish C-Ares Results'
    inputs:
      testRunner: CTest
      testResultsFiles: '**/Test.xml'
      mergeTestResults: true
      testRunTitle: 'cares'
    condition: succeededOrFailed()

- job: linux_mbed
  # Ubuntu 24.04 ships mbedTLS 3.x via libmbedtls-dev, but adapters/tlsio_mbedtls.c
  # in this repo targets the mbedTLS 2.x API (e.g. mbedtls_ssl_get_max_out_record_payload).
  # Install mbedTLS 2.28 (LTS) from source so the link step resolves, mirroring
  # the mbedtls_2_28 job in azure-iot-sdk-c.
  timeoutInMinutes: 60
  pool:
    vmImage: 'ubuntu-24.04'
  displayName: 'mbedTLS'
  steps:
  - checkout: self
    submodules: true
  - script: |
      set -e
      # The Microsoft-hosted ubuntu-24.04 image preinstalls libmbedtls-dev,
      # which on 24.04 is mbedTLS 3.x. The unit test relies on umock-c
      # parsing <mbedtls/ssl.h> to auto-mock mbedTLS symbols used by
      # adapters/tlsio_mbedtls.c (e.g. mbedtls_ssl_get_max_out_record_payload),
      # but that function does not exist in 3.x's headers, so it is left as
      # an undefined external at link time. Purge the system mbedTLS so the
      # 2.28 install under /usr/local is the only one CMake can find.
      sudo apt-get update
      sudo apt-get purge -y 'libmbedtls*' 'libmbedcrypto*' 'libmbedx509*' || true
      sudo apt-get install -y \
        clang \
        cmake \
        build-essential \
        pkg-config \
        curl \
        libcurl4-openssl-dev \
        uuid-dev
    displayName: 'Host setup'
  - script: |
      set -e
      git clone --depth 1 -b mbedtls-2.28.10 https://github.com/Mbed-TLS/mbedtls /tmp/mbedtls-2.28
      cd /tmp/mbedtls-2.28
      mkdir build && cd build
      cmake -DUSE_SHARED_MBEDTLS_LIBRARY=ON -DCMAKE_INSTALL_PREFIX=/usr/local ..
      make -j$(nproc)
      sudo make install
      sudo ldconfig
    displayName: 'Install mbedTLS 2.28'
  - script: |
      chmod +x jenkins/linux_mbed_clang_buildonly.sh
      ./jenkins/linux_mbed_clang_buildonly.sh
    displayName: 'Clang Build'
    env:
      LD_LIBRARY_PATH: /usr/local/lib
  - script: |
      sudo chmod 755 jenkins/linux_mbed.sh
      sudo -E ./jenkins/linux_mbed.sh
    displayName: 'Build'
    env:
      LD_LIBRARY_PATH: /usr/local/lib

- job: linux_mbed_4x
  # mbedTLS 4.x moved all cryptography to TF-PSA-Crypto and removed the public
  # entropy/CTR_DRBG modules, mbedtls_ssl_conf_rng() and
  # mbedtls_ssl_conf_min_version(). adapters/tlsio_mbedtls.c handles that via
  # MBEDTLS_VERSION_NUMBER guards; this job keeps that path compiling.
  timeoutInMinutes: 60
  pool:
    vmImage: 'ubuntu-24.04'
  displayName: 'mbedTLS 4.x'
  steps:
  - checkout: self
    submodules: true
  - script: |
      set -e
      sudo apt-get update
      # Purge the distro mbedTLS 3.x so the 4.x install under /usr/local is the
      # only one CMake can find.
      sudo apt-get purge -y 'libmbedtls*' 'libmbedcrypto*' 'libmbedx509*' || true
      sudo apt-get install -y \
        clang \
        cmake \
        build-essential \
        pkg-config \
        curl \
        libcurl4-openssl-dev \
        uuid-dev \
        python3-pip
    displayName: 'Host setup'
  - script: |
      set -e
      git clone --depth 1 -b mbedtls-4.2.0 https://github.com/Mbed-TLS/mbedtls /tmp/mbedtls-4x
      cd /tmp/mbedtls-4x
      git submodule update --init --recursive --depth 1
      python3 -m pip install --break-system-packages -r scripts/basic.requirements.txt
      mkdir build && cd build
      cmake -DUSE_SHARED_MBEDTLS_LIBRARY=ON -DENABLE_TESTING=Off -DENABLE_PROGRAMS=Off -DCMAKE_INSTALL_PREFIX=/usr/local ..
      make -j$(nproc)
      sudo make install
      sudo ldconfig
    displayName: 'Install mbedTLS 4.2'
  - script: |
      chmod +x jenkins/linux_mbed_clang_buildonly.sh
      ./jenkins/linux_mbed_clang_buildonly.sh
    displayName: 'Clang Build'
    env:
      LD_LIBRARY_PATH: /usr/local/lib
  - script: |
      sudo chmod 755 jenkins/linux_mbed.sh
      sudo -E ./jenkins/linux_mbed.sh
    displayName: 'Build'
    env:
      LD_LIBRARY_PATH: /usr/local/lib

- job: wolfssl
  timeoutInMinutes: 60
  pool:
    vmImage: 'ubuntu-24.04'
  displayName: 'WolfSSL'
  steps:
  - checkout: self
    submodules: true
  - script: |
      set -e
      sudo apt-get update
      sudo apt-get install -y \
        clang \
        cmake \
        build-essential \
        pkg-config \
        curl \
        libcurl4-openssl-dev \
        uuid-dev \
        libwolfssl-dev || true
    displayName: 'Host setup'
  - script: |
      chmod +x jenkins/linux_wolfssl_clang_buildonly.sh
      ./jenkins/linux_wolfssl_clang_buildonly.sh
    displayName: 'Clang Build'
  - script: |
      cmake --version
      cc --version
      gcc --version
      make --version
      LD_LIBRARY_PATH=/usr/local/lib
      LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/my_library/
      sudo jenkins/linux_wolfssl.sh
    displayName: 'Build'

- job: bearssl
  timeoutInMinutes: 60
  pool:
    vmImage: 'ubuntu-24.04'
  displayName: 'BearSSL'
  steps:
  - checkout: self
    submodules: true
  - script: |
      set -e
      sudo apt-get update
      sudo apt-get install -y \
        clang \
        cmake \
        build-essential \
        pkg-config \
        curl \
        libcurl4-openssl-dev \
        uuid-dev \
        libbearssl-dev
    displayName: 'Host setup'
  - script: |
      chmod +x jenkins/linux_bearssl_clang_buildonly.sh
      ./jenkins/linux_bearssl_clang_buildonly.sh
    displayName: 'Clang Build'
  - script: |
      LD_LIBRARY_PATH=/usr/local/lib
      LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/my_library/
      cd $(Build.SourcesDirectory)
      bearsslDir=/beary
      export bearsslDir
      sudo bash jenkins/linux_bearssl.sh
    displayName: 'Build'

- job: ubuntu2404arm
  # ARM64 replacement for the retired ubuntu2204arm self-hosted ARM agent.
  # Microsoft-hosted Azure Pipelines does not offer a generally-available
  # ARM64 Linux vmImage on the default Azure Pipelines agent pool, so we
  # cross-compile with the aarch64-linux-gnu toolchain on an x64 agent to
  # validate ARM64 code generation. Tests are NOT executed here; runtime
  # ARM validation is the responsibility of downstream consumers.
  timeoutInMinutes: 60
  pool:
    vmImage: 'ubuntu-24.04'
  displayName: 'Ubuntu 24.04 ARM64 (Cross-compile only)'
  steps:
  - checkout: self
    submodules: true
  - script: |
      set -e
      sudo dpkg --add-architecture arm64
      # Restrict the default (amd64) sources to amd64 so apt does not try
      # to resolve arm64 packages against the x86_64 mirror (which fails).
      sudo sed -i 's|^deb |deb [arch=amd64] |' /etc/apt/sources.list || true
      if [ -f /etc/apt/sources.list.d/ubuntu.sources ]; then
        sudo sed -i 's|^Types: deb$|Types: deb\nArchitectures: amd64|' /etc/apt/sources.list.d/ubuntu.sources || true
      fi
      CODENAME=$(lsb_release -cs)
      sudo tee /etc/apt/sources.list.d/arm64-ports.list >/dev/null <<EOF
      deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports ${CODENAME} main restricted universe multiverse
      deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports ${CODENAME}-updates main restricted universe multiverse
      deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports ${CODENAME}-security main restricted universe multiverse
      EOF
      sudo apt-get update
      sudo apt-get install -y \
        cmake \
        build-essential \
        pkg-config \
        crossbuild-essential-arm64 \
        libcurl4-openssl-dev:arm64 \
        libssl-dev:arm64 \
        uuid-dev:arm64 \
        zlib1g-dev:arm64
    displayName: 'Install aarch64 cross toolchain and arm64 dev libs'
  - script: |
      set -e
      mkdir -p cmake-arm64
      cd cmake-arm64
      cmake \
        -DCMAKE_SYSTEM_NAME=Linux \
        -DCMAKE_SYSTEM_PROCESSOR=aarch64 \
        -DCMAKE_C_COMPILER=aarch64-linux-gnu-gcc \
        -DCMAKE_CXX_COMPILER=aarch64-linux-gnu-g++ \
        -DCMAKE_LIBRARY_ARCHITECTURE=aarch64-linux-gnu \
        -DCMAKE_FIND_ROOT_PATH="/usr/aarch64-linux-gnu;/usr/lib/aarch64-linux-gnu;/usr/include/aarch64-linux-gnu" \
        -DCMAKE_FIND_ROOT_PATH_MODE_PROGRAM=NEVER \
        -DCMAKE_FIND_ROOT_PATH_MODE_LIBRARY=ONLY \
        -DCMAKE_FIND_ROOT_PATH_MODE_INCLUDE=ONLY \
        -DCMAKE_FIND_ROOT_PATH_MODE_PACKAGE=ONLY \
        -DPKG_CONFIG_LIBDIR=/usr/lib/aarch64-linux-gnu/pkgconfig:/usr/share/pkgconfig \
        -DOPENSSL_ROOT_DIR=/usr/lib/aarch64-linux-gnu \
        -DOPENSSL_INCLUDE_DIR=/usr/include \
        -DOPENSSL_CRYPTO_LIBRARY=/usr/lib/aarch64-linux-gnu/libcrypto.so \
        -DOPENSSL_SSL_LIBRARY=/usr/lib/aarch64-linux-gnu/libssl.so \
        -DCMAKE_BUILD_TYPE=Debug \
        -Drun_unittests:BOOL=OFF \
        ..
      make -j$(nproc)
    displayName: 'Cross-compile c-utility for aarch64'

- job: OSX
  timeoutInMinutes: 60
  variables:
    CodeQL.Enabled: false
    # macOS-14 ships CMake 4.x, which removed compatibility with
    # cmake_minimum_required(VERSION < 3.5). The submodule
    # deps/azure-macro-utils-c/CMakeLists.txt still requests 2.8.x, so we
    # raise the floor via this env var (honored by CMake 3.31+/4.x) instead
    # of patching the submodule.
    CMAKE_POLICY_VERSION_MINIMUM: '3.5'
  pool:
    vmImage: 'macOS-14'
  displayName: 'OSX'
  steps:
  - checkout: self
    submodules: true
  - script: |
      ./jenkins/osx_gcc_openssl.sh
    displayName: 'Build'
  - script: |
      cd cmake && ctest -T test --no-compress-output -C "Debug" -V -j 8 --schedule-random
    displayName: 'Run Tests'
  - task: PublishTestResults@2
    displayName: 'Publish OSX Results'
    inputs:
      testRunner: CTest
      testResultsFiles: '**/Test.xml'
      mergeTestResults: true
      testRunTitle: 'OSX'
    condition: succeededOrFailed()
  - task: ComponentGovernanceComponentDetection@0
    displayName: 'Component Detection'
    condition: always()
  - script: rm -rf $(Agent.BuildDirectory)/*
    displayName: 'Cleanup'
    condition: always()

- job: xcodenative
  timeoutInMinutes: 60
  variables:
    CodeQL.Enabled: false
    # See note on the OSX job above.
    CMAKE_POLICY_VERSION_MINIMUM: '3.5'
  pool:
    vmImage: 'macOS-14'
  displayName: 'XCode Native'
  steps:
  - checkout: self
    submodules: true
  - script: |
      curl --version
      export DYLD_LIBRARY_PATH="/usr/local/Cellar/curl/$(ls -b /usr/local/Cellar/curl | grep -e '[0-9\.]')/lib"
      ./jenkins/osx_xcode_native.sh
    displayName: 'Build'
  - script: |
      export DYLD_LIBRARY_PATH="/usr/local/Cellar/curl/$(ls -b /usr/local/Cellar/curl | grep -e '[0-9\.]')/lib"
      cd cmake && ctest -T test --no-compress-output -C "Debug" -V -j 8 --schedule-random
    displayName: 'Run Tests'
  - task: PublishTestResults@2
    displayName: 'Publish XCode Results'
    inputs:
      testRunner: CTest
      testResultsFiles: '**/Test.xml'
      mergeTestResults: true
      testRunTitle: 'XCode'
    condition: succeededOrFailed()
  - task: ComponentGovernanceComponentDetection@0
    displayName: 'Component Detection'
    condition: always()
  - script: rm -rf $(Agent.BuildDirectory)/*
    displayName: 'Cleanup'
    condition: always()
